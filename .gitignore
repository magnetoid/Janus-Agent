.venv/
venv/
__pycache__/
*.pyc
node_modules/
.torsor/index/
.DS_Store
.venv/
.pytest-cache/
.pytest_cache/
pytest-of-*/

# torsor agent memory / decision log — local-only, not shipped
.torsor/
janus_cli/web_dist/

# desktop app build artifacts (apps/desktop)
apps/desktop/build/
apps/desktop/dist/
apps/desktop/release/
*.tsbuildinfo

# Bootstrap completion marker — written into the repo root by janus_bootstrap.
# Must stay ignored so `janus update`'s `git stash --include-untracked` does
# not sweep it away and re-trigger bootstrap on every update (#38529).
.janus-bootstrap-complete

# Provider keys and deployment secrets. docker-compose reads this file from the
# repo root, so it holds real credentials and must never be committed.
.env
.env.local
